<?xml version="1.0" encoding="UTF-8"?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:maps="clr-namespace:Xamarin.Forms.GoogleMaps;assembly=Xamarin.Forms.GoogleMaps"
    x:Class="TransportControl.MapPage"
    Title="Transport Control">
    <ContentPage.Content>
        <Grid Margin="0,0,0,0" Padding="0,0,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
             <StackLayout Grid.Row="0" Grid.Column="0">
                <maps:Map x:Name="map"
                    InitialCameraUpdate="52.237049, 21.017532, 12, 0, 0"/>
            </StackLayout>

             <StackLayout Grid.Row="0" Grid.Column="0" Orientation="Horizontal" HorizontalOptions="Center" VerticalOptions="Start">
                <Button Command="{Binding GoToLines}"
                        HorizontalOptions="Center"
                        BackgroundColor="White"
                        TextColor="Black"
                        Text="All" />
                <Button Command="{Binding ClearMap}"
                        HorizontalOptions="Center"
                        BackgroundColor="White"
                        TextColor="Black"
                        Text="Clear" />
                <Button Command="{Binding GoToRadius}"
                        HorizontalOptions="Center"
                        BackgroundColor="White"
                        TextColor="Black"
                        Text="Near me" />
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>
